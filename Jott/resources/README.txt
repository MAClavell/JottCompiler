To compile and run:

Navigate to the src directory using cd. Then run these commands:

javac Jott.java
java Jott -program_file_name


To clean the directory of all the class files generated by javac run:

rm *.class